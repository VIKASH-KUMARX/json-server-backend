{
  "name": "json-server-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch src/db.json --port $PORT --host 0.0.0.0",
    "dev": "json-server --watch src/db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
